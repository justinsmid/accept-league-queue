import React from 'react';
import './Dashboard.sass';

export default props => {
    return (
        <div className="page dashboard">
            <p className="placeholder-text">This is the dashboard. It is still under development and does not yet have any useful data to show.</p>
        </div>
    );
};